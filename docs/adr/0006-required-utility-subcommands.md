# 0006 — Required utility subcommands

AgentUtils exposes one `agentutils` command whose focused capabilities are required subcommands; the existing Surface lives at `agentutils editor`, and future utilities join it as siblings. The rename is a clean break: no `agenteditor` executable, MCP resource namespace, state fallback, or installer migration survives.
