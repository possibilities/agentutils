# 0002 — Bare fxnk surface

The live TUI renders only editable Document text and borrows visual roles solely from the fxnk style guide. All commands, state, and recovery UI are transient overlays because persistent chrome would make the editor visually incompatible beside or inside fmx.
